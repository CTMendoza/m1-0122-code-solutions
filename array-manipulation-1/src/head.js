/* exported head */

// define a function named head and give it a parameter called array
// within the declaration block, assign the expression array.length - 1 to a new variable named firstElement
// return the variable firstElement
// outside of the function head, call the fuction with no arguments???

function head(array) {
  var firstElement = array[0];
  return firstElement;
}

// define a new function named head and give it a parameter called array
// within the code block assign the index array[0] to a new variable called firstElement
// return the variable first element
// end the code block of the head function
