/* exported last */
// define a new function called last and give it a parameter called array
// within the code block, assign array[array.length - 1] to the variable named lastElement
// return the variable lastElement

function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}

// define a new function called last and give it a parameter called array
// within the code block, assign array[array.length - 1] to the variable named lastElement
// return the variable lastElement
// end code block for the function last
