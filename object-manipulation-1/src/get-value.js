/* exported getValue */
// declare a function named getValue and give it a parameter  passing two variables named object and key
// in the code block, assign the object variable to a new variable named keyValue
// return keyValue
function getValue(object, key) {
  var keyValue = object;
  return keyValue[key];
}

// declare a function named getValue and give it a parameter  passing two variables named object and key
// in the code block, assign the object variable to a new variable named keyValue
// return the index of keyValue at the variable key
