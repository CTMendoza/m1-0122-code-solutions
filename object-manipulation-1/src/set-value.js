/* exported setValue */
// create a new function called setValue and give it three parameters named object key and value
function setValue(object, key, value) {
  object[key] = value;
  return object;
}

/// create a new function called setValue and give it three parameters named object key and value
// in the code block, assign the value variable to the the object array at index key
// return the object
