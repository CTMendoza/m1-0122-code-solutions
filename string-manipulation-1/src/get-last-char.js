/* exported getLastChar */
// define a new function named getLastChar and give it a parameter named string
// return the value of string.charAt(string.length - 1)
function getLastChar(string) {
  return string.charAt(string.length - 1);
}

// define a new function named getLastChar and give it a parameter named string
// return the value of string.charAt(string.length - 1)
